Need to install the following npm packages before running locally:
    npm install uuid
    npm install moment
    npm install dotenv --save

For Splunk logging:
    npm install npm -g
    npm install --save splunk-logging
